/** @type {import('next').NextConfig} */
// const nextConfig = {}
const nextConfig = {
    images: {
        domains: ["cdn.imagin.studio"]
    }
}
module.exports = nextConfig



//module.exports = nextConfig
